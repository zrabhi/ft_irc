#include "Commands.hpp"
# include "../header.hpp"

// bool Commands::QUIT(Vector params, Iterator &_client)
// {
//     if (params.size() < 2)
//         return (replyto(CLOSINGLINK(_client->second.getNickName()), _client->first),\
//                     close(_client->first), false);
//     String tmp("");
//     appendToParams(params, tmp, 2);
//     replyto(CLOSINGLINK(_client->second.getNickName()), _client->first);
//     _users.erase(_client->first);
//     close(_client->first);
//     return (true);
// }
